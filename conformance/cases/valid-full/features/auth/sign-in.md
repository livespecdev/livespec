# Sign In

When a user submits valid credentials, the system establishes an authenticated session so that protected features become available.

## Requirements

- [x] REQ-1: User can sign in with email and password
  - [x] AC-1.1: Valid credentials establish a session
  - [x] AC-1.2: Invalid credentials are rejected with an inline error
