# API

When a client calls a public endpoint, the system serves a JSON response so that integrations can consume the product programmatically.

## Requirements

- [ ] REQ-1: Public endpoints return JSON
